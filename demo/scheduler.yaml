apiVersion: apps/v1
kind: DaemonSet
metadata:
  namespace: topolvm-system
  name: topolvm-scheduler
spec:
  template:
    spec:
      containers:
        - name: topolvm-scheduler
          command:
            - /topolvm-scheduler
            - --listen=localhost:9251
            - --divisor=0.01
